<%= simple_form_for [@artist, @booking] do |f| %>
  <%= f.input :date, label: "Dates", wrapper_html: { class: "inline_field_wrapper" } %>
  <%= f.submit "book", class: "btn btn-primary btn-lg btn-block btn-booking" %>
<% end %>